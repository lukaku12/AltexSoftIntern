// TODO implement validation handlers, at least that described in `validationHandlers` below

export const ValidationStagesE = {
  BLUR: 'blur',
  SUBMIT: 'submit',
};

export const validationHandlers = {
  // required: validateRequired,
  // email: validateEmail,
  // min: validateMinValue,
  // max: validateMaxValue,
  // minLength: validateMinLength,
  // maxLength: validateMaxLength,
};
